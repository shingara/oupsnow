default: -r features/authentication/steps -r features/step_definitions -r features/support --format pretty features
autotest: -r features/authentication/steps -r features/step_definitions -r features/support --format pretty features
autotest-all: -r features/authentication/steps -r features/step_definitions -r features/support --format progress features
