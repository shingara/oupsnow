default: -r features/steps -r features/support --format pretty features
autotest: -r features/steps -r features/support --format pretty features
autotest-all: -r features/steps -r features/support --format progress features
