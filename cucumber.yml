default: -r features/authentication/steps -r features/steps -r features/support --format pretty features
autotest: -r features/authentication/steps -r features/steps -r features/support --format pretty features
autotest-all: -r features/authentication/steps -r features/steps -r features/support --format progress features
