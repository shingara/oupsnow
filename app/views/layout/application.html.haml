!!! Strict
!!!
%html
  %head
    %title= @title || "Oupsnow"
    = css_include_tag 'oupsnow'
  %body
    #banner
      #header
        %h1= title_project
      #login_nav.nav
        %ul
          - unless session.authenticated?
            %li= link_to('Login', url(:login))
          - else
            %li= link_to('My profile', url(:edit_user, session.user))
          %li.last= link_to('Register', url(:new_user))

    #menu.nav
      %ul
        %li= link_to 'Projects', resource(:projects)
        - unless @project.nil? || @project.id.nil?
          %li= link_to 'Tickets', resource(@project, :tickets)
          %li= link_to 'Add new ticket', resource(@project, :tickets, :new) if session.authenticated?
    %div#content
      = catch_content :for_layout
