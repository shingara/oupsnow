%h1
  States controller, edit action
%p
  Edit this file in
  %tt
    app/views/states/edit.html.erb
= error_messages_for @state

= form_for(@state, :action => resource(:admin, @state)) do
  %p
    = submit "Update"

= link_to 'Show', resource(:admin, @state)
|
= link_to 'Back', resource(:admin, :states)
