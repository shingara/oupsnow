.content
  %h2
    States

  .inner
    .flash
      = flash[:notice]

    - form_tag update_all_admin_states_url, :method => :put, :class => 'form' do
      %table.table
        %thead
          %tr
            %th.first Name
            %th.last Closed?
        %tbody
          - for state in @states
            %tr{:class => cycle('even', 'odd')}
              %td= state.name
              %td= check_box_tag "closed[#{state.id}]", 1, state.closed, :class => 'checkbox'
      .actions-bar
        .actions
          = link_to 'Add states', new_admin_state_url
          = submit_tag "Update all states", :class => 'button'
        .clear
