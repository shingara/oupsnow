%h1 Projects controller, new action

%p 
  Edit this file in 
  %tt app/views/projects/new.html.erb

= error_messages_for :project

= form_for(@project, :action => url(:projects) ) do
  %p
    = submit "Create"

= link_to 'Back', url(:projects)