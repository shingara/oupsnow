%h1 Create a Project

= error_messages_for :project

= form_for(@project, :action => url(:projects) ) do
  
  %p= text_field :name, :label => 'Name'
  %p= label("Description", :for => 'description')
  %p= text_area :description
  %p= submit "Create"

= link_to 'Back', url(:projects)
