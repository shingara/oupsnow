.content
  %h2 Edit your project

  .inner
    .flash
      = error_messages_for :project

    = form_for(@project, :action => url(:project, @project), :class => 'form') do
      = partial :form
      %div{:class => 'group navform'}
        = submit "Update", :class => 'button'
